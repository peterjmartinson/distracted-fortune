version: '3.8'

services:
  jekyll:
    image: ghcr.io/github/pages-gem:latest
    volumes:
      - .:/src/site
    ports:
      - "4000:4000"
    command: jekyll serve --host 0.0.0.0 --source /src/site
