# Legacy — content/posts is no longer used

Articles are now authored directly in `_posts/YYYY-MM-DD-slug.md` (Jekyll format).

The `content/posts/` directory was part of the old WordPress pipeline and is kept
here for historical reference only. No workflow reads from this directory.

To create a new article, run `new-post.ps1` and choose **Article**.
